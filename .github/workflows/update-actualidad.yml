name: Actualizar Actualidad Curro

on:
  schedule:
    - cron: "0 9 * * 1"  # Cada lunes a las 11:00 Madrid (9:00 UTC)
  workflow_dispatch:      # También se puede lanzar manualmente

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run update script
        run: python scripts/update_actualidad.py

      - name: Commit changes if any
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --staged --quiet; then
            echo "Sin cambios. No se hace commit."
          else
            git commit -m "auto: actualización semanal de Actualidad [$(date +'%Y-%m-%d')]"
            git push
            echo "✅ Cambios publicados."
          fi
